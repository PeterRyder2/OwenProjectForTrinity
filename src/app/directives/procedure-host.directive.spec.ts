import { ProcedureHostDirective } from './procedure-host.directive';

describe('ProcedureHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcedureHostDirective();
    expect(directive).toBeTruthy();
  });
});
